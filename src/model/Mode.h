#ifndef MODE_H
#define MODE_H

/**
 * Defines the modes that the game program can be in.
 */
enum Mode {
    Mode_START,
    Mode_MAIN_MENU,
    Mode_NEW_MENU,
    Mode_EXISTING_MENU,
    Mode_OPTIONS_MENU,
    Mode_READ_STRING,
    Mode_DEAD
};

#endif
